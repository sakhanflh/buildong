/* eslint-disable react/prop-types */
const Layout = ({children}) => {
    return(
        <main className="max-w-7xl pb-10 pt-20 mx-auto relative">
            {children}
        </main>
    )
}

export default Layout;

